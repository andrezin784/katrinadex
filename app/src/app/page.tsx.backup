'use client';

import { motion, useScroll, useTransform, AnimatePresence } from 'framer-motion';
// import { ConnectButton } from '@rainbow-me/rainbowkit';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import { ArrowRight, Shield, Lock, Zap, TrendingUp, Sparkles, Globe, Users, DollarSign, Clock, CheckCircle2 } from 'lucide-react';
import Link from 'next/link';
import { useEffect, useState } from 'react';

export default function Home() {
  const { scrollYProgress } = useScroll();
  const y = useTransform(scrollYProgress, [0, 1], [0, -50]);
  const opacity = useTransform(scrollYProgress, [0, 0.5], [1, 0]);
  const [isVisible, setIsVisible] = useState(false);

  useEffect(() => {
    setIsVisible(true);
  }, []);

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-950 via-purple-950 to-slate-900 text-white overflow-hidden">
      {/* Advanced Background Effects */}
      <div className="absolute inset-0">
        {/* Primary gradient mesh */}
        <div className="absolute inset-0 bg-[radial-gradient(circle_at_30%_20%,rgba(139,92,246,0.15),transparent_50%)]" />
        <div className="absolute inset-0 bg-[radial-gradient(circle_at_70%_80%,rgba(0,209,255,0.15),transparent_50%)]" />
        <div className="absolute inset-0 bg-[radial-gradient(circle_at_50%_50%,rgba(168,85,247,0.1),transparent_70%)]" />

        {/* Animated particles */}
        <div className="absolute inset-0">
          {[...Array(100)].map((_, i) => {
            const delay = Math.random() * 10;
            const duration = 8 + Math.random() * 12;
            const size = 1 + Math.random() * 3;

            return (
              <motion.div
                key={i}
                className="absolute rounded-full bg-gradient-to-r from-purple-400/30 to-cyan-400/30 blur-sm"
                style={{
                  width: size,
                  height: size,
                  left: `${Math.random() * 100}%`,
                  top: `${Math.random() * 100}%`,
                }}
                animate={{
                  x: [0, Math.random() * 200 - 100],
                  y: [0, Math.random() * 200 - 100],
                  scale: [1, 1.5, 1],
                  opacity: [0.3, 0.8, 0.3],
                }}
                transition={{
                  duration,
                  delay,
                  repeat: Infinity,
                  repeatType: 'reverse',
                  ease: 'easeInOut',
                }}
              />
            );
          })}
        </div>

        {/* Floating orbs */}
        <motion.div
          className="absolute top-20 left-20 w-32 h-32 bg-purple-500/10 rounded-full blur-3xl"
          animate={{
            scale: [1, 1.2, 1],
            opacity: [0.3, 0.6, 0.3],
          }}
          transition={{ duration: 6, repeat: Infinity }}
        />
        <motion.div
          className="absolute bottom-32 right-32 w-48 h-48 bg-cyan-500/8 rounded-full blur-3xl"
          animate={{
            scale: [1.2, 1, 1.2],
            opacity: [0.2, 0.5, 0.2],
          }}
          transition={{ duration: 8, repeat: Infinity, delay: 2 }}
        />
      </div>

      {/* Premium Navigation */}
      <motion.nav
        className="relative z-20 flex items-center justify-between p-6 backdrop-blur-xl bg-white/5 border-b border-white/10"
        initial={{ y: -100, opacity: 0 }}
        animate={{ y: 0, opacity: 1 }}
        transition={{ duration: 0.8, ease: 'easeOut' }}
      >
        <motion.div
          className="flex items-center space-x-3"
          whileHover={{ scale: 1.02 }}
          transition={{ type: 'spring', stiffness: 300 }}
        >
          <div className="relative">
            <div className="w-10 h-10 bg-gradient-to-br from-purple-500 via-purple-600 to-cyan-500 rounded-xl flex items-center justify-center shadow-2xl shadow-purple-500/25">
              <Lock className="w-5 h-5 text-white" />
            </div>
            <motion.div
              className="absolute -inset-1 bg-gradient-to-r from-purple-500 to-cyan-500 rounded-xl opacity-50 blur-sm"
              animate={{ opacity: [0.5, 0.8, 0.5] }}
              transition={{ duration: 3, repeat: Infinity }}
            />
          </div>
          <div className="flex flex-col">
            <span className="text-xl font-bold bg-gradient-to-r from-purple-400 via-cyan-400 to-purple-400 bg-clip-text text-transparent tracking-tight">
              KatrinaDEX
            </span>
            <span className="text-xs text-gray-400 font-medium">Privacy Mixer</span>
          </div>
        </motion.div>

        <div className="flex items-center space-x-3">
          <motion.div whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}>
            <Link href="/compliance">
              <Button
                variant="ghost"
                className="text-gray-300 hover:text-white hover:bg-white/10 backdrop-blur-sm border border-transparent hover:border-white/20 transition-all duration-300"
              >
                <Shield className="w-4 h-4 mr-2" />
                Compliance
              </Button>
            </Link>
          </motion.div>

          <motion.div
            whileHover={{ scale: 1.05 }}
            whileTap={{ scale: 0.95 }}
            className="relative group"
          >
            <Link href="/deposit">
              <Button className="bg-gradient-to-r from-purple-600 via-purple-700 to-cyan-600 hover:from-purple-500 hover:via-purple-600 hover:to-cyan-500 text-white font-semibold px-6 py-2.5 rounded-xl shadow-2xl shadow-purple-500/25 hover:shadow-purple-500/40 transition-all duration-300 relative overflow-hidden group">
                <div className="absolute inset-0 bg-gradient-to-r from-purple-400 to-cyan-400 opacity-0 group-hover:opacity-20 transition-opacity duration-300" />
                <span className="relative flex items-center">
                  <Sparkles className="w-4 h-4 mr-2" />
                  Start Mixing
                  <ArrowRight className="w-4 h-4 ml-2 group-hover:translate-x-1 transition-transform duration-300" />
                </span>
              </Button>
            </Link>
          </motion.div>

          <motion.div whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}>
            <Button
              variant="outline"
              className="border-2 border-purple-500/50 text-purple-300 hover:text-white hover:bg-purple-500/20 hover:border-purple-400 backdrop-blur-sm transition-all duration-300 font-semibold px-6 py-2.5 rounded-xl"
            >
              <Globe className="w-4 h-4 mr-2" />
              Connect Wallet
            </Button>
          </motion.div>
        </div>
      </motion.nav>

      {/* Hero Section */}
      <section className="relative z-10 px-6 py-32 text-center overflow-hidden">
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ duration: 1.2 }}
          className="max-w-6xl mx-auto"
        >
          {/* Animated Badge */}
          <motion.div
            initial={{ opacity: 0, scale: 0.8, y: 20 }}
            animate={{ opacity: 1, scale: 1, y: 0 }}
            transition={{ duration: 0.8, delay: 0.2 }}
            className="mb-8"
          >
            <Badge className="px-6 py-3 bg-gradient-to-r from-green-500/20 to-emerald-500/20 text-green-300 border border-green-500/30 hover:border-green-400/50 backdrop-blur-sm text-base font-semibold rounded-full shadow-lg shadow-green-500/10">
              <CheckCircle2 className="w-4 h-4 mr-2" />
              100% Compliant with FATF, BACEN & OFAC
              <motion.div
                className="absolute inset-0 bg-gradient-to-r from-green-500/10 to-emerald-500/10 rounded-full"
                animate={{ opacity: [0, 0.5, 0] }}
                transition={{ duration: 3, repeat: Infinity }}
              />
            </Badge>
          </motion.div>

          {/* Main Headline */}
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 1, delay: 0.4 }}
            className="mb-8"
          >
            <h1 className="text-6xl md:text-8xl lg:text-9xl font-black mb-4 tracking-tight">
              <span className="block bg-gradient-to-r from-purple-400 via-cyan-400 to-purple-400 bg-clip-text text-transparent animate-pulse">
                ZK Privacy
              </span>
              <span className="block text-5xl md:text-7xl lg:text-8xl bg-gradient-to-r from-white via-gray-200 to-white bg-clip-text text-transparent">
                Safest on Market
              </span>
            </h1>

            {/* Decorative line */}
            <motion.div
              className="w-32 h-1 bg-gradient-to-r from-purple-500 via-cyan-500 to-purple-500 rounded-full mx-auto mb-6"
              initial={{ scaleX: 0 }}
              animate={{ scaleX: 1 }}
              transition={{ duration: 1.2, delay: 0.8 }}
            />
          </motion.div>

          {/* Subtitle */}
          <motion.p
            className="text-xl md:text-2xl lg:text-3xl text-gray-300 mb-12 max-w-4xl mx-auto leading-relaxed font-light"
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8, delay: 0.6 }}
          >
            The world's most beautiful and compliant privacy mixer.
            <span className="block mt-2 text-lg md:text-xl text-purple-300 font-medium">
              ZK-proof technology + fixed pools + total compliance.
            </span>
          </motion.p>

          {/* CTA Buttons */}
          <motion.div
            className="flex flex-col sm:flex-row gap-6 justify-center items-center mb-16"
            initial={{ opacity: 0, y: 30 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8, delay: 0.8 }}
          >
            <motion.div whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}>
              <Link href="/deposit">
                <Button
                  size="lg"
                  className="group bg-gradient-to-r from-purple-600 via-purple-700 to-cyan-600 hover:from-purple-500 hover:via-purple-600 hover:to-cyan-500 text-white font-bold text-xl px-12 py-6 h-auto rounded-2xl shadow-2xl shadow-purple-500/30 hover:shadow-purple-500/50 transition-all duration-500 relative overflow-hidden"
                >
                  <motion.div
                    className="absolute inset-0 bg-gradient-to-r from-purple-400 to-cyan-400 opacity-0 group-hover:opacity-30 transition-opacity duration-500"
                    initial={false}
                  />
                  <span className="relative flex items-center">
                    <Lock className="w-6 h-6 mr-3" />
                    Start Mixing Now
                    <ArrowRight className="w-6 h-6 ml-3 group-hover:translate-x-2 transition-transform duration-300" />
                  </span>
                </Button>
              </Link>
            </motion.div>

            <motion.div whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}>
              <Link href="/withdraw">
                <Button
                  size="lg"
                  variant="outline"
                  className="group border-2 border-purple-500/50 text-purple-300 hover:text-white hover:bg-purple-500/20 hover:border-purple-400 text-xl px-12 py-6 h-auto rounded-2xl backdrop-blur-sm font-semibold transition-all duration-500"
                >
                  <span className="flex items-center">
                    <Zap className="w-6 h-6 mr-3" />
                    Withdraw Funds
                    <motion.div
                      className="ml-3 w-2 h-2 bg-purple-400 rounded-full"
                      animate={{ opacity: [1, 0.5, 1] }}
                      transition={{ duration: 2, repeat: Infinity }}
                    />
                  </span>
                </Button>
              </Link>
            </motion.div>
          </motion.div>

          {/* Trust Indicators */}
          <motion.div
            className="flex flex-wrap justify-center items-center gap-8 text-gray-400"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ duration: 0.8, delay: 1 }}
          >
            <div className="flex items-center space-x-2">
              <Users className="w-5 h-5 text-purple-400" />
              <span className="font-medium">10,000+ Users</span>
            </div>
            <div className="flex items-center space-x-2">
              <DollarSign className="w-5 h-5 text-green-400" />
              <span className="font-medium">$50M+ Mixed</span>
            </div>
            <div className="flex items-center space-x-2">
              <Clock className="w-5 h-5 text-cyan-400" />
              <span className="font-medium">99.9% Uptime</span>
            </div>
          </motion.div>
        </motion.div>

        {/* Floating elements */}
        <motion.div
          className="absolute top-1/4 left-10 w-20 h-20 border border-purple-500/20 rounded-full"
          animate={{
            rotate: 360,
            scale: [1, 1.1, 1],
          }}
          transition={{
            rotate: { duration: 20, repeat: Infinity, ease: 'linear' },
            scale: { duration: 4, repeat: Infinity },
          }}
        />
        <motion.div
          className="absolute bottom-1/4 right-10 w-16 h-16 border border-cyan-500/20 rounded-full"
          animate={{
            rotate: -360,
            scale: [1, 1.2, 1],
          }}
          transition={{
            rotate: { duration: 15, repeat: Infinity, ease: 'linear' },
            scale: { duration: 3, repeat: Infinity },
          }}
        />
      </section>

        {/* Premium Stats Section */}
        <motion.div
          initial={{ opacity: 0, y: 40 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 1, delay: 0.4 }}
          className="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-5xl mx-auto px-6"
        >
          {[
            {
              value: '$2.4M',
              label: 'Total TVL',
              change: '+12.5%',
              icon: DollarSign,
              color: 'from-green-500 to-emerald-500',
              glowColor: 'shadow-green-500/20'
            },
            {
              value: '1,247',
              label: 'Today\'s Deposits',
              change: '+8.2%',
              icon: TrendingUp,
              color: 'from-purple-500 to-pink-500',
              glowColor: 'shadow-purple-500/20'
            },
            {
              value: '99.9%',
              label: 'Uptime',
              change: '100%',
              icon: CheckCircle2,
              color: 'from-cyan-500 to-blue-500',
              glowColor: 'shadow-cyan-500/20'
            }
          ].map((stat, index) => (
            <motion.div
              key={stat.label}
              initial={{ opacity: 0, y: 30, scale: 0.9 }}
              animate={{ opacity: 1, y: 0, scale: 1 }}
              transition={{ duration: 0.8, delay: 0.6 + index * 0.1 }}
              whileHover={{ y: -5, scale: 1.02 }}
              className="group"
            >
              <Card className={`relative overflow-hidden backdrop-blur-xl bg-white/5 border border-white/10 hover:border-white/20 transition-all duration-500 shadow-2xl ${stat.glowColor} hover:shadow-3xl`}>
                {/* Animated background gradient */}
                <motion.div
                  className={`absolute inset-0 bg-gradient-to-br ${stat.color} opacity-0 group-hover:opacity-10 transition-opacity duration-500`}
                  animate={{ opacity: [0.05, 0.1, 0.05] }}
                  transition={{ duration: 3, repeat: Infinity }}
                />

                {/* Floating particles */}
                <div className="absolute top-4 right-4">
                  <motion.div
                    className="w-2 h-2 bg-current opacity-30 rounded-full"
                    animate={{
                      y: [0, -10, 0],
                      opacity: [0.3, 0.6, 0.3],
                    }}
                    transition={{
                      duration: 2 + index,
                      repeat: Infinity,
                      delay: index * 0.5,
                    }}
                  />
                </div>

                <CardHeader className="relative z-10 pb-4">
                  <div className="flex items-center justify-between mb-2">
                    <stat.icon className={`w-6 h-6 bg-gradient-to-r ${stat.color} bg-clip-text text-transparent`} />
                    <motion.span
                      className={`text-sm font-semibold bg-gradient-to-r ${stat.color} bg-clip-text text-transparent`}
                      animate={{ opacity: [0.7, 1, 0.7] }}
                      transition={{ duration: 2, repeat: Infinity, delay: index }}
                    >
                      {stat.change}
                    </motion.span>
                  </div>

                  <CardTitle className="text-4xl md:text-5xl font-black bg-gradient-to-r from-white via-gray-100 to-white bg-clip-text text-transparent mb-1">
                    {stat.value}
                  </CardTitle>

                  <CardDescription className="text-gray-400 font-medium text-lg">
                    {stat.label}
                  </CardDescription>
                </CardHeader>

                {/* Animated border */}
                <motion.div
                  className={`absolute bottom-0 left-0 h-1 bg-gradient-to-r ${stat.color}`}
                  initial={{ width: 0 }}
                  animate={{ width: '100%' }}
                  transition={{ duration: 1.5, delay: 1 + index * 0.2 }}
                />
              </Card>
            </motion.div>
          ))}
        </motion.div>
      </section>

      {/* Premium Features Section */}
      <section className="relative z-10 px-6 py-32">
        <div className="max-w-7xl mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 1 }}
            viewport={{ once: true }}
            className="text-center mb-20"
          >
            <motion.div
              initial={{ scale: 0 }}
              whileInView={{ scale: 1 }}
              transition={{ duration: 0.6, type: 'spring', stiffness: 200 }}
              viewport={{ once: true }}
              className="inline-block mb-6"
            >
              <div className="w-20 h-20 bg-gradient-to-br from-purple-500 to-cyan-500 rounded-2xl flex items-center justify-center shadow-2xl shadow-purple-500/30">
                <Sparkles className="w-10 h-10 text-white" />
              </div>
            </motion.div>

            <h2 className="text-5xl md:text-7xl font-black mb-6 tracking-tight">
              Why Choose{' '}
              <span className="bg-gradient-to-r from-purple-400 via-cyan-400 to-purple-400 bg-clip-text text-transparent">
                KatrinaDEX
              </span>
              ?
            </h2>

            <motion.p
              className="text-xl md:text-2xl text-gray-300 max-w-3xl mx-auto leading-relaxed font-light"
              initial={{ opacity: 0 }}
              whileInView={{ opacity: 1 }}
              transition={{ duration: 0.8, delay: 0.2 }}
              viewport={{ once: true }}
            >
              Cutting-edge technology combined with total compliance for the privacy you deserve.
            </motion.p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-8 lg:gap-12">
            {[
              {
                icon: Shield,
                title: "ZK-Proof Compliance",
                description: "Mathematical proofs verify legitimate fund origins before each deposit, ensuring regulatory compliance while maintaining privacy.",
                features: ["FATF Compliant", "OFAC Screening", "AML Ready"],
                color: "from-green-500 to-emerald-500",
                bgColor: "from-green-500/10 to-emerald-500/10",
                iconBg: "from-green-500/20 to-emerald-500/20",
                glowColor: "shadow-green-500/25"
              },
              {
                icon: Lock,
                title: "Maximum Privacy",
                description: "Funds are mixed in fixed-value pools, breaking transaction linkability and ensuring true financial privacy.",
                features: ["Fixed Pools", "No Tracing", "Perfect Mixing"],
                color: "from-purple-500 to-pink-500",
                bgColor: "from-purple-500/10 to-pink-500/10",
                iconBg: "from-purple-500/20 to-pink-500/20",
                glowColor: "shadow-purple-500/25"
              },
              {
                icon: Zap,
                title: "Lightning Speed",
                description: "ZK proofs generated in under 4 seconds in-browser, enabling instant privacy without compromising security.",
                features: ["<4s Generation", "Browser Native", "Instant Privacy"],
                color: "from-cyan-500 to-blue-500",
                bgColor: "from-cyan-500/10 to-blue-500/10",
                iconBg: "from-cyan-500/20 to-blue-500/20",
                glowColor: "shadow-cyan-500/25"
              },
              {
                icon: Globe,
                title: "Multi-Chain Power",
                description: "Native Base support with seamless 1inch, LI.FI, and Odos integration for cross-chain privacy.",
                features: ["Base Native", "1inch + LI.FI", "Cross-Chain"],
                color: "from-orange-500 to-red-500",
                bgColor: "from-orange-500/10 to-red-500/10",
                iconBg: "from-orange-500/20 to-red-500/20",
                glowColor: "shadow-orange-500/25"
              }
            ].map((feature, index) => (
              <motion.div
                key={feature.title}
                initial={{ opacity: 0, y: 50, rotateY: -15 }}
                whileInView={{ opacity: 1, y: 0, rotateY: 0 }}
                transition={{
                  duration: 0.8,
                  delay: index * 0.15,
                  type: 'spring',
                  stiffness: 100
                }}
                viewport={{ once: true }}
                whileHover={{
                  y: -8,
                  rotateY: 5,
                  transition: { duration: 0.3 }
                }}
                className="group h-full"
              >
                <Card className={`relative overflow-hidden backdrop-blur-xl bg-gradient-to-br ${feature.bgColor} border border-white/10 hover:border-white/30 transition-all duration-500 shadow-2xl ${feature.glowColor} hover:shadow-3xl h-full`}>
                  {/* Animated background */}
                  <motion.div
                    className={`absolute inset-0 bg-gradient-to-br ${feature.color} opacity-0 group-hover:opacity-5 transition-opacity duration-500`}
                    animate={{ opacity: [0, 0.03, 0] }}
                    transition={{ duration: 4, repeat: Infinity, delay: index }}
                  />

                  {/* Floating elements */}
                  <div className="absolute top-6 right-6">
                    <motion.div
                      className={`w-3 h-3 bg-gradient-to-r ${feature.color} rounded-full`}
                      animate={{
                        scale: [1, 1.2, 1],
                        opacity: [0.5, 1, 0.5],
                      }}
                      transition={{
                        duration: 2,
                        repeat: Infinity,
                        delay: index * 0.3,
                      }}
                    />
                  </div>

                  <CardHeader className="relative z-10 pb-6">
                    <motion.div
                      className={`inline-flex p-4 rounded-2xl bg-gradient-to-br ${feature.iconBg} mb-6 shadow-lg`}
                      whileHover={{ scale: 1.1, rotate: 5 }}
                      transition={{ type: 'spring', stiffness: 300 }}
                    >
                      <feature.icon className={`w-8 h-8 bg-gradient-to-r ${feature.color} bg-clip-text text-transparent`} />
                    </motion.div>

                    <CardTitle className="text-2xl md:text-3xl font-bold text-white mb-3 group-hover:text-transparent group-hover:bg-gradient-to-r group-hover:bg-clip-text group-hover:from-white group-hover:to-gray-200 transition-all duration-500">
                      {feature.title}
                    </CardTitle>
                  </CardHeader>

                  <CardContent className="relative z-10 space-y-4">
                    <p className="text-gray-300 text-lg leading-relaxed">
                      {feature.description}
                    </p>

                    {/* Feature tags */}
                    <div className="flex flex-wrap gap-2 pt-2">
                      {feature.features.map((tag, tagIndex) => (
                        <motion.span
                          key={tag}
                          className={`px-3 py-1 text-sm font-semibold bg-gradient-to-r ${feature.color} bg-clip-text text-transparent border border-current/20 rounded-full`}
                          initial={{ opacity: 0, scale: 0.8 }}
                          whileInView={{ opacity: 1, scale: 1 }}
                          transition={{
                            duration: 0.5,
                            delay: 0.8 + index * 0.1 + tagIndex * 0.1
                          }}
                          viewport={{ once: true }}
                          whileHover={{ scale: 1.05 }}
                        >
                          {tag}
                        </motion.span>
                      ))}
                    </div>
                  </CardContent>

                  {/* Animated border */}
                  <motion.div
                    className={`absolute bottom-0 left-0 h-1 bg-gradient-to-r ${feature.color}`}
                    initial={{ width: 0 }}
                    whileInView={{ width: '100%' }}
                    transition={{ duration: 1.5, delay: 1 + index * 0.2 }}
                    viewport={{ once: true }}
                  />
                </Card>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* Live Activity Feed */}
      <section className="relative z-10 px-6 py-32">
        <div className="max-w-6xl mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 1 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <motion.div
              initial={{ scale: 0 }}
              whileInView={{ scale: 1 }}
              transition={{ duration: 0.6, type: 'spring', stiffness: 200 }}
              viewport={{ once: true }}
              className="inline-block mb-6"
            >
              <div className="w-16 h-16 bg-gradient-to-br from-cyan-500 to-purple-500 rounded-2xl flex items-center justify-center shadow-2xl shadow-cyan-500/30">
                <motion.div
                  animate={{ rotate: 360 }}
                  transition={{ duration: 2, repeat: Infinity, ease: 'linear' }}
                >
                  <Globe className="w-8 h-8 text-white" />
                </motion.div>
              </div>
            </motion.div>

            <h2 className="text-4xl md:text-6xl font-black mb-6 tracking-tight">
              Live Privacy Network
            </h2>

            <motion.p
              className="text-xl text-gray-300 max-w-2xl mx-auto"
              initial={{ opacity: 0 }}
              whileInView={{ opacity: 1 }}
              transition={{ duration: 0.8, delay: 0.2 }}
              viewport={{ once: true }}
            >
              Real-time anonymous withdrawals happening across the network
            </motion.p>
          </motion.div>

          <motion.div
            initial={{ opacity: 0, y: 40 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 1, delay: 0.3 }}
            viewport={{ once: true }}
          >
            <Card className="backdrop-blur-xl bg-black/20 border border-white/10 shadow-2xl overflow-hidden">
              {/* Header */}
              <div className="p-6 border-b border-white/10 bg-gradient-to-r from-purple-500/10 to-cyan-500/10">
                <div className="flex items-center justify-between">
                  <div className="flex items-center space-x-3">
                    <motion.div
                      animate={{ opacity: [1, 0.5, 1] }}
                      transition={{ duration: 2, repeat: Infinity }}
                      className="w-3 h-3 bg-green-400 rounded-full"
                    />
                    <span className="text-lg font-semibold text-white">Live Activity Feed</span>
                  </div>
                  <div className="text-sm text-gray-400">
                    Updated every 30 seconds
                  </div>
                </div>
              </div>

              <CardContent className="p-0">
                <div className="divide-y divide-white/5">
                  {[
                    {
                      amount: "1.0 ETH",
                      time: "2 min ago",
                      status: "Success",
                      hash: "0x7a8b...3f2e",
                      pool: "Pool 1",
                      color: "from-green-500 to-emerald-500"
                    },
                    {
                      amount: "5.0 ETH",
                      time: "5 min ago",
                      status: "Success",
                      hash: "0x9c1d...8g5h",
                      pool: "Pool 5",
                      color: "from-purple-500 to-pink-500"
                    },
                    {
                      amount: "0.5 ETH",
                      time: "8 min ago",
                      status: "Success",
                      hash: "0x4e6f...2k9m",
                      pool: "Pool 1",
                      color: "from-cyan-500 to-blue-500"
                    },
                    {
                      amount: "10.0 ETH",
                      time: "12 min ago",
                      status: "Success",
                      hash: "0x3b5c...7p1q",
                      pool: "Pool 6",
                      color: "from-orange-500 to-red-500"
                    },
                    {
                      amount: "1.0 ETH",
                      time: "15 min ago",
                      status: "Success",
                      hash: "0x8f2a...6d4r",
                      pool: "Pool 1",
                      color: "from-green-500 to-emerald-500"
                    },
                  ].map((withdrawal, index) => (
                    <motion.div
                      key={index}
                      initial={{ opacity: 0, x: -30 }}
                      whileInView={{ opacity: 1, x: 0 }}
                      transition={{
                        duration: 0.6,
                        delay: index * 0.1,
                        type: 'spring',
                        stiffness: 100
                      }}
                      viewport={{ once: true }}
                      className="group p-6 hover:bg-white/5 transition-all duration-300"
                    >
                      <div className="flex items-center justify-between">
                        <div className="flex items-center space-x-4">
                          {/* Status indicator */}
                          <motion.div
                            className={`w-4 h-4 rounded-full bg-gradient-to-r ${withdrawal.color} shadow-lg`}
                            animate={{ scale: [1, 1.2, 1] }}
                            transition={{
                              duration: 2,
                              repeat: Infinity,
                              delay: index * 0.2
                            }}
                          />

                          {/* Amount */}
                          <div>
                            <div className="font-bold text-white text-lg">
                              {withdrawal.amount}
                            </div>
                            <div className="text-sm text-gray-400 font-mono">
                              {withdrawal.hash}
                            </div>
                          </div>

                          {/* Pool badge */}
                          <div className={`px-3 py-1 bg-gradient-to-r ${withdrawal.color} bg-clip-text text-transparent border border-current/20 rounded-full text-sm font-semibold`}>
                            {withdrawal.pool}
                          </div>
                        </div>

                        <div className="text-right">
                          <div className="text-green-400 font-semibold flex items-center">
                            <CheckCircle2 className="w-4 h-4 mr-1" />
                            {withdrawal.status}
                          </div>
                          <div className="text-sm text-gray-400">
                            {withdrawal.time}
                          </div>
                        </div>
                      </div>

                      {/* Animated progress bar */}
                      <motion.div
                        className={`h-0.5 bg-gradient-to-r ${withdrawal.color} mt-3 rounded-full`}
                        initial={{ width: 0 }}
                        whileInView={{ width: '100%' }}
                        transition={{ duration: 1.5, delay: 0.5 + index * 0.1 }}
                        viewport={{ once: true }}
                      />
                    </motion.div>
                  ))}
                </div>
              </CardContent>

              {/* Footer */}
              <div className="p-4 bg-gradient-to-r from-purple-500/5 to-cyan-500/5 border-t border-white/10">
                <div className="flex items-center justify-center space-x-4 text-sm text-gray-400">
                  <span>🔒 Privacy Protected</span>
                  <span>•</span>
                  <span>⚡ Real-time Updates</span>
                  <span>•</span>
                  <span>🌐 Global Network</span>
                </div>
              </div>
            </Card>
          </motion.div>
        </div>
      </section>

      {/* Premium CTA Section */}
      <section className="relative z-10 px-6 py-32 overflow-hidden">
        {/* Background effects */}
        <div className="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent" />
        <div className="absolute top-0 left-1/2 transform -translate-x-1/2 w-96 h-96 bg-purple-500/10 rounded-full blur-3xl" />

        <motion.div
          initial={{ opacity: 0, y: 50 }}
          whileInView={{ opacity: 1, y: 0 }}
          transition={{ duration: 1, type: 'spring', stiffness: 100 }}
          viewport={{ once: true }}
          className="max-w-6xl mx-auto text-center relative z-10"
        >
          {/* Icon */}
          <motion.div
            initial={{ scale: 0, rotate: -180 }}
            whileInView={{ scale: 1, rotate: 0 }}
            transition={{ duration: 0.8, type: 'spring', stiffness: 200 }}
            viewport={{ once: true }}
            className="inline-block mb-8"
          >
            <div className="w-24 h-24 bg-gradient-to-br from-purple-500 via-cyan-500 to-purple-500 rounded-3xl flex items-center justify-center shadow-2xl shadow-purple-500/30 relative">
              <Lock className="w-12 h-12 text-white" />
              <motion.div
                className="absolute inset-0 bg-gradient-to-r from-purple-400 to-cyan-400 rounded-3xl"
                animate={{ opacity: [0, 0.3, 0] }}
                transition={{ duration: 3, repeat: Infinity }}
              />
            </div>
          </motion.div>

          {/* Main headline */}
          <motion.h2
            className="text-5xl md:text-7xl lg:text-8xl font-black mb-8 tracking-tight leading-tight"
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8, delay: 0.2 }}
            viewport={{ once: true }}
          >
            Ready to Make Your Money
            <span className="block bg-gradient-to-r from-purple-400 via-cyan-400 to-purple-400 bg-clip-text text-transparent animate-pulse">
              Truly Private?
            </span>
          </motion.h2>

          {/* Subtitle */}
          <motion.p
            className="text-xl md:text-2xl text-gray-300 mb-12 max-w-3xl mx-auto leading-relaxed"
            initial={{ opacity: 0 }}
            whileInView={{ opacity: 1 }}
            transition={{ duration: 0.8, delay: 0.4 }}
            viewport={{ once: true }}
          >
            Join thousands of users who already trust KatrinaDEX for their financial privacy.
            Experience the future of compliant cryptocurrency privacy.
          </motion.p>

          {/* CTA Buttons */}
          <motion.div
            className="flex flex-col sm:flex-row gap-6 justify-center items-center mb-16"
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8, delay: 0.6 }}
            viewport={{ once: true }}
          >
            <motion.div whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}>
              <Link href="/deposit">
                <Button
                  size="lg"
                  className="group bg-gradient-to-r from-purple-600 via-purple-700 to-cyan-600 hover:from-purple-500 hover:via-purple-600 hover:to-cyan-500 text-white font-bold text-2xl px-16 py-8 h-auto rounded-3xl shadow-2xl shadow-purple-500/40 hover:shadow-purple-500/60 transition-all duration-500 relative overflow-hidden"
                >
                  <motion.div
                    className="absolute inset-0 bg-gradient-to-r from-purple-400 to-cyan-400 opacity-0 group-hover:opacity-30 transition-opacity duration-500"
                    initial={false}
                  />
                  <span className="relative flex items-center">
                    Start Mixing Now
                    <motion.div
                      className="ml-4"
                      animate={{ x: [0, 5, 0] }}
                      transition={{ duration: 1.5, repeat: Infinity }}
                    >
                      <ArrowRight className="w-8 h-8" />
                    </motion.div>
                  </span>
                </Button>
              </Link>
            </motion.div>

            <motion.div whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}>
              <Link href="/withdraw">
                <Button
                  size="lg"
                  variant="outline"
                  className="group border-2 border-purple-500/50 text-purple-300 hover:text-white hover:bg-purple-500/20 hover:border-purple-400 text-xl px-12 py-6 h-auto rounded-2xl backdrop-blur-sm font-semibold transition-all duration-500"
                >
                  <span className="flex items-center">
                    Withdraw Funds
                    <motion.div
                      className="ml-3 w-2 h-2 bg-purple-400 rounded-full"
                      animate={{ opacity: [1, 0.5, 1] }}
                      transition={{ duration: 2, repeat: Infinity }}
                    />
                  </span>
                </Button>
              </Link>
            </motion.div>
          </motion.div>

          {/* Trust indicators */}
          <motion.div
            className="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-4xl mx-auto"
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8, delay: 0.8 }}
            viewport={{ once: true }}
          >
            {[
              { icon: Shield, text: "100% Compliant" },
              { icon: Lock, text: "ZK Privacy" },
              { icon: Zap, text: "Lightning Fast" }
            ].map((item, index) => (
              <motion.div
                key={item.text}
                className="flex items-center justify-center space-x-3 text-gray-300"
                whileHover={{ scale: 1.05 }}
                transition={{ type: 'spring', stiffness: 300 }}
              >
                <item.icon className="w-6 h-6 text-purple-400" />
                <span className="font-semibold">{item.text}</span>
              </motion.div>
            ))}
          </motion.div>
        </motion.div>
      </section>

      {/* Premium Footer */}
      <footer className="relative z-10 backdrop-blur-xl bg-black/20 border-t border-white/10">
        <div className="max-w-7xl mx-auto px-6 py-16">
          <div className="grid grid-cols-1 md:grid-cols-4 gap-12 mb-12">
            {/* Brand */}
            <div className="md:col-span-2">
              <motion.div
                className="flex items-center space-x-3 mb-6"
                whileHover={{ scale: 1.02 }}
                transition={{ type: 'spring', stiffness: 300 }}
              >
                <div className="w-12 h-12 bg-gradient-to-br from-purple-500 to-cyan-500 rounded-2xl flex items-center justify-center shadow-2xl shadow-purple-500/30">
                  <Lock className="w-6 h-6 text-white" />
                </div>
                <div>
                  <span className="text-2xl font-black bg-gradient-to-r from-purple-400 to-cyan-400 bg-clip-text text-transparent">
                    KatrinaDEX
                  </span>
                  <div className="text-sm text-gray-400">Privacy Mixer</div>
                </div>
              </motion.div>

              <p className="text-gray-300 mb-6 max-w-md leading-relaxed">
                The world's most beautiful and compliant privacy mixer.
                Experience the future of cryptocurrency privacy with ZK technology.
              </p>

              <div className="flex space-x-4">
                {[
                  { icon: '🐦', label: 'Twitter' },
                  { icon: '💬', label: 'Discord' },
                  { icon: '📖', label: 'Docs' }
                ].map((social, index) => (
                  <motion.button
                    key={social.label}
                    className="w-12 h-12 bg-white/10 hover:bg-white/20 rounded-xl flex items-center justify-center text-white transition-all duration-300 hover:scale-110"
                    whileHover={{ y: -2 }}
                    whileTap={{ scale: 0.95 }}
                  >
                    <span className="text-lg">{social.icon}</span>
                  </motion.button>
                ))}
              </div>
            </div>

            {/* Links */}
            <div>
              <h3 className="text-white font-bold text-lg mb-6">Product</h3>
              <div className="space-y-4">
                {['Deposit', 'Withdraw', 'Dashboard', 'Compliance'].map((item) => (
                  <motion.div
                    key={item}
                    whileHover={{ x: 5 }}
                    transition={{ type: 'spring', stiffness: 300 }}
                  >
                    <Link href={`/${item.toLowerCase()}`} className="text-gray-400 hover:text-white transition-colors duration-300">
                      {item}
                    </Link>
                  </motion.div>
                ))}
              </div>
            </div>

            {/* Support */}
            <div>
              <h3 className="text-white font-bold text-lg mb-6">Support</h3>
              <div className="space-y-4">
                {['Documentation', 'Help Center', 'Contact', 'Status'].map((item) => (
                  <motion.div
                    key={item}
                    whileHover={{ x: 5 }}
                    transition={{ type: 'spring', stiffness: 300 }}
                  >
                    <Link href={`/${item.toLowerCase().replace(' ', '-')}`} className="text-gray-400 hover:text-white transition-colors duration-300">
                      {item}
                    </Link>
                  </motion.div>
                ))}
              </div>
            </div>
          </div>

          {/* Bottom bar */}
          <motion.div
            className="pt-8 border-t border-white/10"
            initial={{ opacity: 0 }}
            whileInView={{ opacity: 1 }}
            transition={{ duration: 0.8 }}
            viewport={{ once: true }}
          >
            <div className="flex flex-col md:flex-row justify-between items-center">
              <div className="text-gray-400 text-sm mb-4 md:mb-0">
                © 2025 KatrinaDEX. All rights reserved.
              </div>

              <div className="flex items-center space-x-6 text-sm text-gray-400">
                <span>Built with ❤️ for privacy</span>
                <span>•</span>
                <span>ZK Technology</span>
                <span>•</span>
                <span>Base Network</span>
              </div>
            </div>
          </motion.div>
        </div>

        {/* Animated background elements */}
        <div className="absolute bottom-0 left-0 w-full h-1 bg-gradient-to-r from-purple-500 via-cyan-500 to-purple-500" />
        <motion.div
          className="absolute bottom-4 right-8 w-2 h-2 bg-purple-400 rounded-full"
          animate={{
            scale: [1, 1.5, 1],
            opacity: [0.5, 1, 0.5],
          }}
          transition={{ duration: 2, repeat: Infinity }}
        />
      </footer>
    </div>
  );
}
